package es.anescdev.velox.context.sumatory.model.enums;

/** Estado de un sumatorio (actualmente no usado como estado persistido de la entidad, ver también {@code EntityState}). */
public enum SumatoryState {
    SAVED, DRAFT;
}
